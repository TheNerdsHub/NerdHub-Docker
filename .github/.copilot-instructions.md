This repository contains the Docker Compose configuration for the NerdHub application stack.

- It orchestrates the backend, frontend, discord bot, and mongo database services.
- The main configuration file is `docker-compose.yml`.
- Environment variables are managed in the `.env` file, which is created from `stack.env`.
- To run the stack: `docker-compose up --build`.
- To stop the stack: `docker-compose down`.